/**
 * Copie este arquivo para config.js e preencha com os dados do Supabase.
 * O config.js esta no .gitignore e nao vai para o repositorio.
 *
 * Na Vercel, prefira injetar via script de build ou variaveis de ambiente.
 */
export const config = {
  supabaseUrl: 'https://SEU_PROJECT_ID.supabase.co',
  supabaseAnonKey: 'sua_chave_anon_publica',
  useSupabase: false
};
